<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.festa.dao.MemberDAO">

    <insert id="insertMemberInfo" parameterType="com.festa.dto.MemberDTO">
        insert into member_test(username, password, email, confirmEmail)
            values (#{username}, #{password}, #{email}, #{confirmEmail})
    </insert>

    <!-- EXISTS()에서 true는 Alias가 1로, false는 Alias가 0으로 되어있다.  -->
    <select id="idIsDuplicated" resultType="int">
        select EXISTS (select username from member_test
                       where username = #{username}) as isDuplicated
    </select>
</mapper>
